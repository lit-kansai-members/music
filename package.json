{
  "name": "lit-music",
  "private": true,
  "version": "2.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "predev": "yarn format",
    "build": "yarn format && tsc && vite build",
    "preview": "vite preview",
    "format": "prettier --write .",
    "check": "prettier --check . && tsc --noEmit"
  },
  "dependencies": {
    "lit": "^3.1.2",
    "marked": "^15.0.0"
  },
  "devDependencies": {
    "cssnano": "^9.0.0",
    "postcss": "^8.4.38",
    "postcss-preset-env": "^11.0.0",
    "prettier": "^3.8.3",
    "typescript": "^7.0.0",
    "vite": "^8.0.0",
    "vite-plugin-checker": "^0.14.0"
  }
}
